server {
    listen 80;
    server_name _;
    root /usr/share/nginx/html;
    index index.html;

    # SPA fallback: unmatched paths (client-side routes) serve index.html.
    location / {
        try_files $uri /index.html;
    }
}
