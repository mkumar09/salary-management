# Frontend

React + Vite + MUI. See the [repo root README](../README.md) for setup, environment variables,
and how this connects to the backend.

```bash
npm install
npm run dev     # http://localhost:5173
npm run test
npm run build
```
